---
name: shortstory-write
description: 中短篇滚动写。逐章写->字数门禁->盲审->单章循环->全篇终审。所有章节在同一正文.md，## 分隔。
allowed-tools: Read Write Edit Grep Glob Bash Task
argument-hint: "[类型/项目名] [--resume]"
---

# 中短篇 · 滚动写

## 核心约定

- 所有章节在 `短篇/{类型}/{项目名}/正文.md`，## 第N章：标题 分隔
- 进度由 write_status.json 驱动，不靠对话记忆

## 格式约束

- 章节之间只用 `## 第N章：标题` 分隔，**禁止**使用 `---` 或其他 markdown 分隔线
- 正文中禁止任何形式写 `---` 作为章节或段落分割
- 禁止使用 `\n\n` 作为叙述文本中出现的显式标记
- 段落间距由自然换行控制，不需要额外分隔线
- 例外：系统界面、电子屏幕内容（如手机屏幕、电脑界面、聊天记录等）可以使用 `---` 来表示屏幕切换，除此之外一律不用

## 启动（唯一入口）

```bash
python <skill_dir>/start.py 短篇/{类型}/{项目名}/ [--resume]
```

这是每次会话**必须最先运行**的命令。不要跳过。

start.py 自动执行：
1. 字数门禁校验
2. 读状态文件确定当前进度
3. 打印下一步该做什么

所有后续决策基于 start.py 的输出，而非对话记忆。

## 流程概述

```
写一章 -> validate_chapter.py 字数门禁 -> 字数不够就修
   字数够 -> in_review -> 子agent盲审
       零阻断 -> passed -> 下一章
       有阻断 -> blocking -> 修 -> 重新校验
全部写完 -> 全篇盲审 -> 零阻断+零警告 -> 定稿
```

## 校验

```bash
python <skill_dir>/validate_chapter.py 短篇/{类型}/{项目名}/
```

字数未达目标（实际 < 目标）报错阻拦。

## 模式

| 模式 | 说明 |
|------|------|
| 默认 | 从第1章开始 |
| --resume | 续写，读状态文件从断点继续 |